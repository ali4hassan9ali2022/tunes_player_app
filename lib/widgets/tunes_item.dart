import 'package:flutter/material.dart';

class TunesItem extends StatelessWidget {
  const TunesItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: GestureDetector(
        onTap: () {},
        child: Container(
          
        ),
      ),
    );
  }
}